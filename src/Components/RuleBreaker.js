import React from 'react';

const style = {
  title: {
    color: 'white',
    padding: '5px',
  },
};

export default function RuleBreaker() {
  return (
    <div style={style.title}>
      <h1>No Rules are broken so far.</h1>
    </div>
  );
}
